import React, {Component} from "react";
import gypRockTypes from "GypRock";

class PBoard extends React {
    constructor(props) {
        super(props);
        this.state = {
            count: 0,
        }
    }

    //function to increase state weight

    // function for how many multiplier

    componentDidMount() 
    // logic to add to count state

    render() {
        
        return (
            <div>
                <button onClick={() }>

                </button>
            </div>
        )
    }
}

